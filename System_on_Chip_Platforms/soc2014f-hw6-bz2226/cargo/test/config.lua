sys = {
   devices = {
      --debayer0 = {
      --type = "debayer",
      -- max_size_log = 15,
      --},
      devayer0 = {
	      type = "debayer-sync",
	      -- this is synthesisable and thus doesn't have configurable max size
      },
   },
}
